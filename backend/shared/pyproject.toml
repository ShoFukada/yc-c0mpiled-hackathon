[project]
name = "yc-hackathon-shared"
version = "0.1.0"
description = "Shared settings and utilities"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.9.1",
]

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
